# 义乌可信数据空间 · 数据跨域流通平台 需求文档（用户端 + 管理后台）

| 项目名称 | 数据跨域流通平台 |
| --- | --- |
| 所属项目 | 义乌可信数据空间 |
| 文档版本 | v1.3（用户端 + 管理后台） |
| 技术形态 | 纯前端 HTML 原型（HTML + CSS + JavaScript，数据存储于浏览器 localStorage） |
| 编写日期 | 2026-08-14 |

> **说明**：本需求文档描述**用户端（门户端）**与**独立管理后台**两部分。用户端面向公众与企业主体，提供浏览、申请提交、进度与结果查询、咨询反馈、工作台概览等能力，**不含任何审批操作**；各类申请（平台入驻、授权运营商入驻、授权场景、数据资源、数据产品出域、应用调用、运营人员备案等）的**审批与管理均由独立管理后台完成**。

---

## 1. 项目概述

### 1.1 项目背景

当前数据孤岛现象严重，跨机构、跨域的数据流通普遍存在"不敢共享、不愿共享、不会共享"的问题。本平台（用户端）面向普通用户与企业主体，提供数据产品的发现、获取，授权运营场景的了解与申请，数据资源目录的浏览，数据咨询反馈，以及授权运营商入驻申请等服务，是公众与企业接触数据跨域流通服务的主要入口。

### 1.2 建设目标

1. 提供统一门户，支持数据产品、授权场景、数据资源目录的浏览与检索。
2. 支持企业主体在线提交平台账号入驻申请，并实时查看申请进度与结果。
3. 支持企业主体在线提交授权运营商入驻申请，并实时查看申请进度。
4. 提供数据咨询与反馈渠道（目录有误、数据需求、其他），支持查看平台处理结果。
5. 所有申请提交后进入独立管理后台审批，用户端仅展示申请状态。
6. 管理后台面向平台运营与授权运营单位管理人员，提供授权场景管理、数据资源申请审批、数据产品管理、出域管理、应用管理、运营人员备案、账号管理等全流程审批与管理能力。

### 1.3 术语定义

| 术语 | 说明 |
| --- | --- |
| 授权运营单位 | 完成平台入驻并通过授权运营审批的企业主体 |
| 授权运营商 | 平台生态合作伙伴，可申请入驻成为授权运营商 |
| 授权运营场景 | 授权运营单位开展数据开发利用的特定业务场景，由管理后台审批 |
| 数据产品 | 基于授权数据资源加工形成、可上架交易的产品（数据集 / API / 数据报告 / 数据应用） |
| 数据资源目录 | 平台统一梳理的数据资源清单，含字段级结构说明 |

### 1.4 系统边界

| 范围 | 说明 |
| --- | --- |
| 用户端（门户端） | 浏览、搜索、申请提交、进度与结果查询、咨询反馈、工作台业务概览（无审批） |
| 管理后台（独立审批端） | 授权场景管理、数据资源申请审批、数据产品管理、出域管理、应用管理、运营人员备案、账号管理等 |

> 用户端所有提交的操作，最终审批均发生在独立的管理后台；用户端只展示审批后的状态（待审核 / 已通过 / 已驳回）及驳回原因。用户端工作台仅做统计展示与业务节点概览，不含任何审批操作；管理后台负责全部审批与管理操作。

---

## 2. 角色与权限（用户端）

| 角色 | 说明 | 用户端可访问功能 |
| --- | --- | --- |
| 访客 / 普通用户 | 未登录或登录平台门户的用户 | 门户首页、数据产品浏览、授权场景、数据资源目录、数据咨询 |
| 申请主体（企业） | 申请平台入驻 / 授权运营商入驻的企业 | 上述功能 + 平台入驻申请、入驻申请记录、授权运营商入驻申请与记录 |
| 授权运营单位 | 已完成入驻的企业用户 | 上述功能 + 工作台（业务概览与提交记录）+ 授权运营商生态相关申请 |

> 审批角色（平台审核员、管理员等）归属于独立的管理后台，不在用户端体现。

---

## 3. 功能需求

### 3.1 登录页（index.html）

#### 功能说明
用户端登录入口，支持账号密码登录与 CA 证书登录两种方式（演示）。

#### 字段与规则

| 字段 | 校验规则 |
| --- | --- |
| 账号 | 必填 |
| 密码 | 必填 |

#### 交互说明
- 登录成功后进入工作台（dashboard.html）。
- 提供"注册账号"入口，跳转平台账号入驻申请页（register.html）。
- 提供"忘记密码"入口（提示联系管理员重置）。

---

### 3.2 平台账号入驻申请（register.html）

#### 功能说明
企业主体三步引导式在线注册申请，填写账号信息、主体信息、上传材料并提交。

#### 步骤一：填写账号信息

| 字段 | 是否必填 | 校验规则 |
| --- | --- | --- |
| 用户名 | 是 | 必填 |
| 密码 | 是 | 至少 6 位 |
| 使用人手机号 | 是 | 11 位手机号格式；点击"获取验证码"发送（模拟） |
| 验证码 | 是 | 必填 |
| 使用人姓名 | 是 | 必填 |
| 身份证号 | 是 | 18 位身份证格式 |
| 邮箱 | 否 | 邮箱格式 |
| 区域 | 是 | 浙江省各地市下拉选择 |
| 浙里办法人认证 | 是 | 显示认证状态，点击"立即去认证"打开认证弹窗，模拟完成法人认证 |
| 入驻平台角色 | 是 | 下拉选择（管理员 / 数据开发 / 运营） |

#### 步骤二：填写主体与上传材料

- **企业主体信息**：运营主体名称、统一社会信用代码、法定代表人姓名、注册地、法定代表人身份证号、注册资本（万元）、营业期限、经营范围（下拉选择）。
- **是否经办人代办**：单选"是，由经办人代办 / 否，法定代表人本人办理"；选择"是"时需填写经办人身份证号、姓名、手机号。
- **上传材料**：
  - 主体资格证明：营业执照（必传）、授权委托书（必传）
  - 数据运营能力证明：运营申请表（必传）、数据安全承诺书（必传）、安全风险自评报告（必传）、其他资质证明（选传）
  - 格式要求：doc、docx、pdf，单个文件不超过 50MB

#### 步骤三：申请提交完成
- 展示提交成功状态："平台将尽快完成审核，审核结果将通过短信通知"。
- 点击"返回登录"跳转登录页。

#### 交互说明
- 顶部进度条指引（账号信息 → 主体信息与材料 → 提交完成），可回退上一步。
- 提供"⚡ 一键填充测试数据"按钮快速演示。
- 各字段实时校验，错误提示并聚焦。
- 提交后申请进入**管理后台审批**，用户端可在"入驻申请记录"中查看进度与结果。

---

### 3.3 入驻申请记录（register-records.html）

#### 功能说明
查看本企业平台入驻申请的进度与审批结果（审批由管理后台完成）。

#### 统计卡片
全部申请、审核中、已通过、已驳回。

#### 列表字段
主体名称、统一社会信用代码、使用人姓名、入驻平台角色、申请时间、状态。

#### 申请状态（管理后台审批结果）
- 待审核（⏳）
- 已通过（✓）
- 已驳回（✕）

#### 功能点
- 关键词搜索（主体名称 / 统一信用代码）。
- 状态筛选。
- 查看详情弹窗：完整申请信息、材料清单、审批结果。
- 申请被驳回时，详情展示**驳回原因**，支持"重新申请"（回填原表单后再次提交）。

---

### 3.4 门户首页 / 数据产品（home.html）

#### 功能说明
数据产品对外展示门户，支持浏览、搜索、行业分类筛选。

#### 顶部导航
首页、数据产品、授权场景、数据资源目录、数据咨询、需求大厅、授权运营商入驻。

#### 页面结构
- **Hero 品牌区**：平台标题、宣传语、关键词搜索框。
- **数据产品卡片网格**：产品名称、产品描述、标签、上架时间、行业分类、产品形态、价格（¥ + 计费单位）。
- **行业分类筛选**：按行业分类切换产品列表。

#### 产品卡片操作
- 加入购物车：模拟加入购物车。
- 立即购买：模拟购买流程。

---

### 3.5 授权场景（scenarios.html）

#### 功能说明
面向公众展示已通过管理后台审批、进入正常运营的授权场景，支持按领域浏览与申请授权。

#### 页面结构
- 场景领域筛选：全部、工业制造、现代农业、商贸流通、交通运输、金融服务、文化旅游、医疗健康、应急管理、城市治理、生态环境。
- 场景列表卡片：场景名称、所属领域、运营单位、场景描述、上线时间、授权运营期（上线时间 ~ 3 年后）。

#### 交互说明
- 点击场景卡片跳转场景详情页（scenario-detail.html）。
- 仅展示管理后台审批通过（状态为"passed"）的场景。

---

### 3.6 场景详情（scenario-detail.html）

#### 功能说明
展示单个授权运营场景的详细信息，供用户了解场景内容。

#### 内容模块
- 场景基本信息：场景名称、所属领域、运营单位、上线时间。
- 场景描述。
- 所需数据资源明细表：序号、数据名称、数源单位。

---

### 3.7 数据资源目录（data-catalog.html）

#### 功能说明
统一的数据资源目录，支持按行业分类与资源类型浏览、查看资源详情与字段结构，并提供数据咨询入口。

#### 页面结构
- 行业分类筛选（含"展开/收起"）。
- 资源类型筛选：全部、数据包、数据接口、数据集、数据报告、其他。
- 资源卡片列表：资源标题、描述、标签、数据来源、行业分类、资源类型、上架时间。

#### 交互说明
- 点击资源卡片打开**资源详情**（列表/详情视图切换）：
  - 基本信息：资源名称、行业分类、资源类型、资源描述。
  - 字段结构表：字段名、字段备注、字段类型、字段长度。
- 提供"数据咨询"入口，打开数据咨询弹窗。

---

### 3.8 数据咨询（consult-records.html）

#### 功能说明
提供数据咨询与反馈渠道，分为目录有误、数据需求、其他三类；支持发起咨询、查看咨询详情与平台处理结果。

#### 左侧分类
全部、目录有误、数据需求、其他（默认进入"全部"）。

#### 列表字段
序号、标题/名称、问题描述、资产名称、反馈时间、状态（待处理 / 已处理）。

#### 新增咨询弹窗（按咨询类型动态切换表单）

| 类型 | 表单字段 |
| --- | --- |
| 目录有误 | 姓名、联系电话、邮箱、问题标题（≤30 字）、数源部门（自动带出）、问题描述（≤500 字）、附件上传（最多 3 个，每个 ≤50M） |
| 数据需求 | 需求名称（≤30 字）、需求描述（≤500 字）、数据用途（≤300 字）、联系方式 |
| 其他 | 咨询内容、联系方式 |

#### 详情弹窗（分组展示）
- **基本信息**：记录编号、咨询类型、资产名称、反馈时间、状态标签。
- **咨询内容**：按类型展示对应字段，附件以文件标签形式展示。
- **处理结果**：平台处理完成后展示"处理回复"（蓝色高亮卡片）；未处理则不展示。

#### 数据规则
- 记录编号格式：JC（目录有误）/ XQ（数据需求）/ CZ（其他）+ 日期 + 序号。
- 咨询提交后由平台后台处理，处理完成后状态变为"已处理"并填写处理回复，用户端可查看。
- 内置 6 条默认测试数据（覆盖三类咨询、待处理/已处理状态），首次访问自动写入。

---

### 3.9 授权运营商招募（operator-register.html）

#### 功能说明
面向生态合作伙伴的授权运营商招募宣传页。

#### 页面结构
- 品牌招募 Hero 区："授权运营商生态合作伙伴招募中…"。
- 加入价值四卡片：直面海量高价值需求、共享可信基础设施、融入数据生态、依托平台合规保障。
- 底部 CTA："成为授权运营商生态合作伙伴"。

#### 交互说明
- 查看入驻信息（跳转 operator-info.html）。
- 发起入驻申请（跳转 operator-apply.html）。

---

### 3.10 授权运营商入驻信息（operator-info.html）

#### 功能说明
展示授权运营商入驻条件、流程、材料要求、领域范围等说明信息，供申请主体了解入驻规则后决定是否申请。

---

### 3.11 授权运营商入驻申请（operator-apply.html）

#### 功能说明
申请主体在线填写入驻信息并提交授权运营商申请（审批由管理后台完成）。

#### 表单字段

| 字段 | 是否必填 | 校验规则 |
| --- | --- | --- |
| 运营商名称 | 是 | 必填 |
| 统一社会信用代码 | 是 | 18 位格式（正则校验） |
| 法定代表人 | 是 | 必填 |
| 申请领域 | 是 | 多选（checkbox 组），至少选择一项 |
| 经营范围 | 否 | 下拉选择 |
| 联系人 | 是 | 必填 |
| 联系电话 | 是 | 11 位手机号格式 |

#### 交互说明
- 提交后生成申请编号（OP + 年月日时分秒），状态进入"提交申请"（待审核），跳转入驻记录页。
- 支持"重新申请"：从既有记录回填表单，便于驳回后修改重提。

---

### 3.12 授权运营商入驻记录（operator-records.html）

#### 功能说明
查看授权运营商入驻申请的进度（审批由管理后台完成）。

#### 列表字段
申请编号、运营商名称、申请领域、联系人、申请时间、当前进度状态。

#### 进度流转（管理后台推进，用户端查看）
提交申请 → 资质审核 → 安全评估 → 入驻开通

#### 交互说明
- 查看详情：完整申请信息 + 当前进度 + 审批结果（驳回时展示驳回原因）。
- 被驳回后可"重新申请"跳转申请页回填表单。

---

### 3.13 工作台（dashboard.html）

#### 功能说明
登录后进入的业务总览页面，面向授权运营单位提供经营统计、业务节点运行状态监测、授权运营全流程节点概览与提交记录展示。**本页面仅做统计展示与流程概览，不含任何审批操作**，各环节的审批均在独立管理后台完成。

#### 统计卡片
本月累计收入、业务主体总数、昨日平台数据产品交易额、上架数据产品个数。

#### 业务节点运行状态监测
在线节点数（如 13 / 13）、在线连接器数（如 11 / 11）、今日数据流量（如 15.7TB）。

#### 授权运营业务节点概览（六节点流程）

| 节点 | 状态 | 说明 |
| --- | --- | --- |
| 1. 场景申报 | 待申报 | 确认场景使用范围与所需数据资源，提交独立审批后台审批 |
| 2. 数据资源申请 | 待申请 | 基于已通过场景，申请所需数据资源使用授权 |
| 3. 数据产品开发 | 待开发 | 利用已授权的数据资源开发数据产品 |
| 4. 数据产品出域审核 | 待审核 | 产品发布提交出域审核，独立审批后台审批通过后方可上架及应用调用 |
| 5. 数据产品上架 | 待上架 | 审核通过的产品上架，场景对外展示关联产品与数据资源清单 |
| 6. 应用产品调用申请 | 待开通 | 应用提交数据产品调用申请，独立审批后台审批通过后生成密钥供调用 |

#### 交互说明
- 每个节点可点击跳转至对应功能页面（场景申报 / 数据资源申请 / 数据产品开发 / 出域审核 / 上架 / 应用调用）。
- 节点状态仅作展示（待申报 / 待申请 / 待开发 / 待审核 / 待上架 / 待开通），状态变化由独立管理后台推进。

#### 提交记录
- 展示本主体最近的业务提交记录列表：提交主体、提交事项、提交时间。
- 示例记录：授权运营商入驻、资源上架删除验证、产品信息展示验证、新增产品、连接器资源、数据资源上传等。

---

## 4. 管理后台需求

管理后台为**独立审批与管理端**，面向平台管理员、审核员及授权运营单位管理人员。管理后台负责各类申请的审批与业务管理，审批结果通过共享的 localStorage 数据联动反馈至用户端（用户端仅展示结果，不做审批）。管理后台导航：首页（工作台）、授权场景管理、数据资源申请、数据产品管理、出域管理、应用管理、运营人员备案、账号管理。

### 4.1 管理后台首页（dashboard.html）

#### 功能说明
管理后台登录后的总览页（导航"首页"），与用户端工作台共用同一页面文件。用户端视角仅做统计展示与节点概览（见 3.13）；管理后台视角侧重于全局运营状态查看与各管理模块入口跳转。

#### 统计卡片
本月累计收入、业务主体总数、昨日平台数据产品交易额、上架数据产品个数。

#### 业务节点运行状态监测
在线节点数（如 13 / 13）、在线连接器数（如 11 / 11）、今日数据流量（如 15.7TB）。

#### 授权运营业务节点概览（六节点流程）
场景申报 → 数据资源申请 → 数据产品开发 → 数据产品出域审核 → 数据产品上架 → 应用产品调用申请。各节点状态（待申报 / 待申请 / 待开发 / 待审核 / 待上架 / 待开通）由管理后台审批推进。

#### 交互说明
- 每个节点可点击跳转至对应管理功能页（授权场景管理 / 数据资源申请 / 数据产品管理 / 出域管理 / 应用管理）。
- 展示本主体最近的业务提交记录：提交主体、提交事项、提交时间。
- 首页仅展示全局状态与入口，审批操作在对应管理页完成。

---

### 4.2 授权场景管理（scenario-management.html）

#### 功能说明
管理授权运营场景的全生命周期：新增场景申报、场景审批、场景查看。场景数据（`scenarioRecords`）与用户端（scenarios、scenario-detail）、数据资源申请、数据产品管理联动。

#### 统计卡片
全部场景、申报审核中、申报已通过、申报已驳回。

#### 场景列表字段
场景编号、场景名称、所属领域、使用范围、所需数据资源、申报时间、状态（申报审核中 / 申报已通过 / 申报已驳回）、操作（查看详情 / 审批）。

#### 新增授权场景（大表单，按"授权运营应用场景说明"分组）

| 分组 | 字段 |
| --- | --- |
| 场景基本信息 | 应用场景名称*、场景领域*（下拉）、场景概述* |
| 场景概况 | 行业背景、申报单位介绍、问题分析、需求分析（与现有应用 / 竞品对比） |
| 应用场景描述 | 场景介绍、数据使用情况、数据加工逻辑、算法模型、接口类产品出入参、运营规划、投入预算、建设进度计划、效益评估（社会 / 经济）、风险与安全保障措施 |
| 授权运营数据使用范围 | 数据使用范围（仅用于哪些环节 / 用途、禁止用途） |
| 所需数据资源 | 多选所需数据源；审批通过后用于"场景详情 → 申请数据资源" |

#### 审批操作
- **审核通过**：状态 → 申报已通过，场景进入用户端正常运营展示（scenarios.html）。
- **审核驳回**：填写驳回原因，状态 → 申报已驳回，申报单位修改后重新提交。

#### 与数据资源申请的联动
- 场景审批通过后，列表操作列出现"申请数据资源"入口，点击携带场景 ID 跳转 `apply-records.html?scenario=<场景ID>`，发起申请时自动选中该场景。
- 场景申报时勾选的"所需数据资源（数据源）"即该场景可申请数据资源的候选范围。
- 场景详情弹窗回显该场景已审批通过的数据资源申请及对应资源清单。

---

### 4.3 数据资源申请（apply-records.html）

#### 功能说明
管理授权数据资源申请全流程：基于已通过场景发起申请、审批、记录查询。

#### 列表字段
申请编号、数据资源名称、数据提供单位、授权运营场景、申请时间、状态（待审核 / 已通过 / 已驳回）、操作（查看详情 / 审批 / 去开发 / 下载回执）。

#### 发起数据资源申请（分区表单）

| 分组 | 内容 |
| --- | --- |
| 选择授权运营场景 | 下拉选择已通过审批的授权场景（未关联数据资源的场景不可提交） |
| 选择数据资源 | 多选数据资源，展示数据提供单位与字段清单 |
| 申请数据范围（共用信息） | 申请数据范围、数据用途、数据安全等级 |
| 所需提交材料 | 合规承诺书、数据安全承诺书等材料清单 |

#### 审批操作
- 审批通过后支持"去开发"（进入数据产品开发 / 发布）。
- 审批驳回时填写驳回原因。
- 支持下载申请回执（含申请字段清单，TXT 格式）。

#### 与授权场景管理的联动关系

数据资源申请必须建立在**已审批通过**的授权场景之上，两者联动规则如下：

| 环节 | 联动规则 |
| --- | --- |
| 场景申报 | 在授权场景管理中申报场景时预选"所需数据资源（数据源）"，构成该场景后续可申请数据资源的候选范围 |
| 场景审批 | 场景仅"申报已通过"后方可发起数据资源申请；"申请数据资源"入口仅对已通过场景开放 |
| 发起申请 | 申请表单自动加载全部已通过场景，场景列表跳转进入时自动带出指定场景（`?scenario=<场景ID>`）；场景未关联任何数据资源时提示"该场景未关联数据资源，无法提交申请" |
| 申请范围 | 可选数据资源受场景预选数据源约束，申请时按数据资源多选并确认字段级申请范围 |
| 审批通过 | 申请审批通过后操作栏出现"去开发"，携带申请编号与场景 ID 跳转数据产品管理，基于该申请创建数据产品 |
| 结果回显 | 场景详情回显该场景已通过的数据资源申请，形成"授权场景 → 数据资源申请 → 数据产品"完整链路 |

---

### 4.4 数据产品管理（data-products.html）

#### 功能说明
管理数据产品全生命周期：产品发布、API 配置与测试、出域状态跟踪、上架 / 下架。产品须通过出域审核后方可上架。

#### 产品列表字段
产品名称、产品编号、行业分类、产品类型（数据集 / API 产品 / 数据报告 / 数据应用）、定价、计费方式、关联场景、使用数据资源、上架状态（已上架 / 已下架）、出域状态、操作。

#### 发布数据产品

| 字段 | 说明 |
| --- | --- |
| 产品名称、产品编号 | 必填 |
| 行业分类 | 下拉选择 |
| 产品类型 | 数据集 / API 产品 / 数据报告 / 数据应用 |
| 定价、计费方式 | 必填（按次调用 / 包年等） |
| 关联场景 | 场景申报通过后可选 |
| 使用数据资源 | 数据资源申请通过后可选 |
| 产品描述、数据字段、备注 | 描述产品内容与字段结构 |

#### API 图形化配置
API 名称、请求路径、入参 / 出参字段配置、调用方式，保存后生成 API 配置。

#### API 接口测试
请求地址、请求方式、请求参数（JSON）、点击"发送请求"查看测试结果。

#### 上架 / 下架
- 仅出域审核通过的产品可上架。
- 下架需填写下架原因（下架管理弹窗）。

---

### 4.5 出域管理（outbound-management.html）

#### 功能说明
管理数据出域全流程：提交出域申请、审核、记录查询。数据产品出域审核通过后方可上架及应用调用。

#### 列表字段
申请编号、出域场景类型、数据敏感类型、关联数据产品、申请时间、状态（待审核 / 已通过 / 已驳回）、操作（查看详情 / 审批）。

#### 提交出域申请

| 字段 | 说明 |
| --- | --- |
| 出域场景类型* | 数据出域 / 模型出域 / API 出域 / 报告出域 |
| 数据敏感类型* | 下拉选择 |
| 关联数据产品 | 关联已发布的数据产品 |
| 出域产品信息 | 产品信息补充说明 |
| 出域申请表* | 文件上传 |
| 加工情况说明* | 文件上传 |
| 专家评审会意见* | 文件上传 |

#### 审批操作
审核通过 / 驳回（填写审核意见），审核意见在详情中展示。

---

### 4.6 应用管理（app-management.html）

#### 功能说明
管理社会应用接入与数据产品调用，含三个 Tab：应用接入申请、数据产品调用申请、应用调用日志。

#### Tab 1：应用接入申请
- 列表字段：应用名称、应用编码、应用类型、对接单位、使用场景、申请时间、审批状态、操作。
- 支持审批（通过 / 驳回）。

#### Tab 2：数据产品调用申请
- 数据产品通过出域审核并上架后，应用可申请调用。
- 列表字段：申请编号、数据产品、申请应用、对接单位、使用用途、申请时间、审批状态、操作。
- 审批通过后生成调用密钥供应用调用。

#### Tab 3：应用调用日志
- 列表字段：调用应用、数据产品名称、类型、累计调用、当日调用、调用成功率、错误次数、最近调用时间、操作。
- 支持查看调用明细。

---

### 4.7 运营人员备案（operators.html）

#### 功能说明
管理授权运营单位运营人员的备案与准入，备案提交后进入独立准入审核流程。

#### 列表字段
姓名、身份证号、手机号码、角色（管理员 / 数据开发 / 运营）、备案状态、提交时间、操作。

#### 添加运营人员

| 字段 | 说明 |
| --- | --- |
| 姓名*、身份证号*、手机号码* | 必填 |
| 分配角色* | 管理员 / 数据开发 / 运营 |
| 身份证照片* | 正反面照片上传 |
| 背景调查报告* | 文件上传 |
| 备注说明 | 选填 |

#### 准入审核与账号开通
- 备案提交后进入独立审批后台准入审核。
- 审核通过后开通登录账号：用户名、登录账号、登录密码、账号角色（密码通过短信发送给运营人员）。

---

### 4.8 账号管理（account-management.html）

#### 功能说明
管理授权运营单位的企业账号：新增账号、角色分配、权限配置、启用 / 禁用。

#### 列表字段
姓名、账号、手机号、角色、状态（启用 / 禁用）、创建时间、操作（编辑角色 / 详情 / 启用禁用）。

#### 新增账号 / 编辑角色权限

| 字段 | 说明 |
| --- | --- |
| 姓名*、账号*、手机号* | 必填（账号唯一性校验） |
| 角色类型* | 管理员 / 数据开发 / 运营 / 自定义角色 |
| 自定义角色名称 | 角色类型为"自定义角色"时必填 |
| 功能权限 | 复选框多选（账号管理、数据产品、运营人员、数据咨询等） |
| 备注 | 选填 |

#### 交互说明
- 新增账号：初始密码通过短信发送至手机号（模拟）。
- 账号唯一性校验：账号已存在时提示更换。
- 启用 / 禁用：禁用后该账号不可登录。

---

## 5. 用户端业务流程

### 5.1 平台入驻申请流程
1. 用户进入登录页 → 点击"注册账号"。
2. 填写账号信息（含浙里办法人认证）→ 选择平台角色。
3. 填写主体信息并上传材料。
4. 提交申请 → 申请进入**管理后台审批**，用户端状态"待审核"。
5. 用户可在"入驻申请记录"中查看进度与结果（已通过 / 已驳回）。
6. 被驳回时查看驳回原因，可"重新申请"。

### 5.2 授权运营商入驻申请流程
1. 招募页查看入驻信息 / 发起申请。
2. 填写运营商信息与申请领域 → 提交申请（生成 OP 编号）。
3. 申请进入**管理后台审批**，用户端展示进度：提交申请 → 资质审核 → 安全评估 → 入驻开通。
4. 被驳回时可"重新申请"（回填原表单）。

### 5.3 数据咨询流程
1. 用户在数据资源目录 / 门户发起咨询（目录有误 / 数据需求 / 其他）。
2. 咨询记录进入列表（状态：待处理）。
3. **平台后台处理**后状态变为"已处理"并填写处理回复。
4. 用户可在详情中查看处理回复。

### 5.4 授权场景 / 数据产品 / 数据资源目录使用流程
1. 用户浏览数据产品、授权场景、数据资源目录，支持搜索与分类筛选。
2. 授权场景仅展示管理后台审批通过的场景。
3. 数据产品支持加入购物车 / 立即购买（演示）。
4. 对资源目录有疑问时可发起数据咨询。

### 5.5 工作台使用流程
1. 登录后进入工作台，查看经营统计（本月收入、业务主体、产品交易额、上架产品数）。
2. 查看业务节点运行状态（在线节点、在线连接器、今日数据流量）。
3. 通过授权运营业务节点概览了解当前各环节状态，点击节点跳转对应功能页。
4. 查看本主体最近的提交记录。
5. 工作台仅展示状态与记录，各环节审批由独立管理后台完成。

---

## 6. 数据存储设计（原型）

原型为纯前端演示，数据存储于浏览器 localStorage：

| localStorage 键名 | 用途 | 涉及页面 |
| --- | --- | --- |
| `registerRecords` | 平台入驻申请记录 | register、register-records |
| `operatorRecords` | 授权运营商入驻记录 | operator-apply、operator-records |
| `scenarioRecords` | 授权运营场景记录（含审批状态；用户端仅展示审批通过场景） | scenario-management、scenarios、scenario-detail、apply-records |
| `dataApplyRecords` | 数据资源申请记录 | apply-records |
| `dataProducts` | 数据产品记录（含出域 / 上架状态） | data-products |
| `outboundRecords` | 出域申请记录 | outbound-management |
| `appRecords` | 应用接入与调用申请记录 | app-management |
| `operatorPersonnel` | 运营人员备案记录 | operators |
| `accounts` | 企业账号记录 | account-management |
| `consultRecords` | 数据咨询记录（含处理回复） | data-catalog、consult-records |

> 用户端与管理后台共用 `scenarioRecords` 等数据：管理后台推进审批状态，用户端读取并仅展示审批通过的结果。

---

## 7. 非功能需求

| 类别 | 要求 |
| --- | --- |
| 视觉风格 | 政务蓝主题色，卡片化布局，统一顶部导航、筛选栏、卡片、弹窗组件 |
| 响应式 | 适配桌面端为主，768px 以下隐藏导航菜单、内容降为单列 |
| 表单校验 | 手机号（11 位）、身份证（18 位）、统一社会信用代码（18 位正则）、密码（≥6 位）、邮箱格式等；校验失败显示错误提示并聚焦 |
| 易用性 | 空状态展示、一键填充测试数据、步骤进度条、字数统计（标题/描述类输入） |
| 状态展示 | 用户端所有申请类流程仅展示状态与结果（待审核 / 已通过 / 已驳回），不提供审批操作；审批操作仅存在于管理后台 |
| 审批留痕 | 管理后台审批须填写审批意见 / 驳回原因，审批结果与意见在两端均可查看 |
| 数据一致性 | 授权场景仅展示管理后台审批通过的场景；咨询处理回复由后台填写后展示；数据产品须出域审核通过后方可上架 |
| 扩展性 | 页面结构统一，数据结构按业务域分离存储，便于后续接入真实后端接口 |

---

## 8. 页面清单

| 序号 | 页面 | 功能 |
| --- | --- | --- |
| 1 | index.html | 登录页（账号密码 / CA 证书） |
| 2 | dashboard.html | 工作台（统计卡片 + 节点运行监测 + 六节点流程概览 + 提交记录，无审批） |
| 3 | register.html | 平台账号入驻申请（三步向导 + 法人认证 + 材料上传） |
| 4 | register-records.html | 入驻申请记录（搜索/筛选/详情/驳回原因/重新申请） |
| 5 | home.html | 门户首页 / 数据产品浏览搜索筛选 |
| 6 | scenarios.html | 授权场景列表（领域筛选 + 仅展示已通过场景） |
| 7 | scenario-detail.html | 授权场景详情（数据资源明细） |
| 8 | data-catalog.html | 数据资源目录（字段结构详情 + 咨询入口） |
| 9 | consult-records.html | 数据咨询（三类咨询 + 详情 + 处理回复） |
| 10 | operator-register.html | 授权运营商招募宣传页 |
| 11 | operator-info.html | 授权运营商入驻信息 |
| 12 | operator-apply.html | 授权运营商入驻申请（OP 编号） |
| 13 | operator-records.html | 授权运营商入驻记录（四步进度） |

> 工作台已包含在用户端需求范围（仅统计展示与节点概览，无审批）；管理后台的审批类页面（授权场景管理、数据资源申请、数据产品管理、出域管理、应用管理、运营人员备案、账号管理）及全部审批操作不在用户端需求范围。
